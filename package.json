{
    "name": "vcs-inventory",
    "version": "1.0.0",
    "description": "API for vcs-inventory",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
      "serverless": "^3.17.0"
    },
    "overrides": {
      "serverless": "^3.17.0"
    }
  }
  